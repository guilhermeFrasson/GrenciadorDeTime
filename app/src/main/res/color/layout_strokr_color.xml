<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor quando o campo está focado -->
    <item android:color="@color/corTexto" android:state_focused="true"/>
    <!-- Cor padrão (quando não está focado) -->
    <item android:color="@color/corTexto" android:state_focused="false"/>
</selector>
